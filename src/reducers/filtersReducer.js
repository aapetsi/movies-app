const filtersReducerDefaultState = {
  text: '',
  sortBy: 'date'
}

export const filtersReducer = (state = filtersReducerDefaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
