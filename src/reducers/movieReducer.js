const initialState = {
  movies: [],
  savedMovies: [],
  watchedMovies: []
}

export const movieReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
